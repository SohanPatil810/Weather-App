export default function Footer() {
  return (
    <footer className="mx-auto mb-8 w-[min(1120px,calc(100%-2rem)))] text-center text-sm text-slate-400">
      <p>Built with React, Tailwind CSS, Framer Motion, and OpenWeatherMap API.</p>
      <p className="mt-2">Use the search above to discover weather around the world.</p>
    </footer>
  );
}
